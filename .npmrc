# Align with pnpm-workspace.yaml nodeLinker: hoisted.
# Hoisted layout is the most reliable for Next.js + OpenNext on Windows
# (avoids deep symlink trees that break under path limits / antivirus).
node-linker=hoisted
public-hoist-pattern[]=*
